monolog:
  handlers:
    main:
      type: fingers_crossed
      action_level: error
      handler: nested
      excluded_http_codes: [401, 403, 404, 422]

    nested:
      type: stream
      path: "php://stderr"
      level: debug
